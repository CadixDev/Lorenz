dependencies {
    compile "org.cadixdev:bombe:${rootProject.bombeVersion}"

    testCompile project(':lorenz-dsl-groovy')
}

javadoc {
    options.encoding = 'UTF-8'
    options.charSet = 'UTF-8'
    options.links(
            'https://docs.oracle.com/javase/8/docs/api/'
    )
}
