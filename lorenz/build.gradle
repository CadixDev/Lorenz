dependencies {
    compile "me.jamiemansfield:bombe:${rootProject.bombeVersion}"
}

javadoc {
    options.encoding = 'UTF-8'
    options.charSet = 'UTF-8'
    options.links(
            'https://docs.oracle.com/javase/8/docs/api/'
    )
}
